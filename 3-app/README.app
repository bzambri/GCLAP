# Application for GCLAP

The user interface is a dash app served with gunicorn.

![GCLAP UI](/images/GCLAP_app.png)

The app consists of a single Python script (`app.py`) that translates user input into SQL queries, and retrieves and displays the results.

TO DO: Replace the (proof of concept) ncread with the SQL read from the database
